extends Resource

class_name Modifier

@export var mod_name: String
@export var mod_type: String
@export var mod_value: float 
@export var mult: bool # if yes, then apply the modifier multiplicitively. if not, apply additively
